#include<stdio.h>
int findsmallest(int arr[],int n);
void main()
{
	int arr[]={5,2,6,1,3};
	for(i=0;i<n;i++)
	{
		printf("enter elements");
		scanf("%d",&arr[i]);
	}
	int smallest=a[0]
	for(i=1;i<n;i++)
	{
		if(arr[i]<smallest)
		smallest=arr[i];
	}
	printf("%d",smallest);
	return 0;
}
